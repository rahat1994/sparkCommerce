<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up()
    {
        $variationsTable = strval(config("sparkcommerce.table_prefix")).strval(config("sparkcommerce.product_variants_table_name"));

        if (! Schema::hasTable($variationsTable)) {
            return;
        }

        if (! Schema::hasColumn($variationsTable, 'stock_quantity')) {
            Schema::table($variationsTable, function (Blueprint $table) {
                $table->integer('stock_quantity')->nullable();
            });
        }

        if (! Schema::hasColumn($variationsTable, 'attribute_combination')) {
            Schema::table($variationsTable, function (Blueprint $table) {
                $table->json('attribute_combination')->nullable();
            });
        }
    }

    public function down()
    {
        $variationsTable = strval(config("sparkcommerce.table_prefix")).strval(config("sparkcommerce.product_variants_table_name"));

        if (! Schema::hasTable($variationsTable)) {
            return;
        }

        if (Schema::hasColumn($variationsTable, 'stock_quantity')) {
            Schema::table($variationsTable, function (Blueprint $table) {
                $table->dropColumn('stock_quantity');
            });
        }

        if (Schema::hasColumn($variationsTable, 'attribute_combination')) {
            Schema::table($variationsTable, function (Blueprint $table) {
                $table->dropColumn('attribute_combination');
            });
        }
    }
};
